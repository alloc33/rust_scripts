## In this file I will list different kind of things which I can code completely from the scratch (from memory)

- DSA/leetcode problems

- HTTP Server (std Rust, no tokio).
- TCP Server (std Rust, no tokio).
- TCP Client (std Rust, no tokio).

- Higher-Ranked Trait Bounds.
